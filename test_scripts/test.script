var memo = Table();
fn fib(n) {
  if (n < 2) return n;
  var m = memo.get(n);
  if (m != null) {
    return m;
  }
  var a = fib(n - 1) + fib(n - 2);
  memo.put(n, a);
  return a;
}

var start = clock_milli();
//var sum = 0;
//while (sum < 100) {
//    fib(sum);
//    sum = sum + 1;
//}

print fib(1000);
print (clock_milli() - start)/1000;