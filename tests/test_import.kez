import nested_import "nested_import.kez";

fn test_function(a,b) {
    print a + b;
}

class Person {
    fn init(name, age) {
        self.name = name;
        self.age = age;
    }
}

var hello = "hello from test import";

var nested = nested_import.nested_hello;