var hello = "hello ";
var world = "world!";
var hw = hello + world;
print hw;
hello = "bon jour ";
world = "planet!";
print hello + world;
fn fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = clock_milli();
print fib(35);
print (clock_milli() - start)/1000;